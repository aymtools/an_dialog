name: an_dialogs
description: "Used to manage route-based dialogs and loading states, controlling when they are pushed and
dismissed."
version: 1.3.0
homepage: https://github.com/aymtools/an_dialog/
issue_tracker: https://github.com/aymtools/an_dialog/issues

topics:
  - loading
  - dialog
  - dialog-manager
  - dialogs

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cancellable: ^2.5.0
  weak_collections: ^1.5.1
  anlifecycle: ^3.6.1
  an_lifecycle_cancellable: ^2.6.0-pre.1
  an_async_data: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter: